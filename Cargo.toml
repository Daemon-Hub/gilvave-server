[workspace]
resolver = "3"

members = [
    "crates/core",
    "crates/infra",
    "crates/messaging",
    "crates/settings",
    "crates/s3",

    "services/gateway",
    "services/http-api",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.96"

[workspace.dependencies]

# --- async/runtime ---
tokio = { version = "1.52.1", features = ["full"] }
tokio-util = "0.7.18"

# --- web ---
axum = { version = "0.8.9", features = ["ws", "multipart"] }
tower-http = { version = "0.6.8", features = ["trace", "fs"] }

# --- serialization ---
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"

# --- observability ---
tracing = "0.1.44"
tracing-subscriber = { version = "0.3.23", features = [
    "fmt",
    "env-filter",
    "chrono",
    "json",
] }

# --- utils ---
uuid = { version = "1.23.1", features = ["v4", "v7", "serde"] }
anyhow = "1.0.102"
validator = { version = "0.20", features = ["derive"] }
dotenvy = "0.15.7"
time = { version = "0.3.47", features = ["serde"] }
thiserror = "2.0.18"
futures = "0.3.32"
bytes = "1.11.1"
image = "0.25.10"
mime_guess = "2.0.5"

# --- database ---
sqlx = { version = "0.8.6", features = [
    "runtime-tokio-native-tls",
    "postgres",
    "macros",
    "uuid",
    "time",
    "migrate",
] }

# --- auth ---
jsonwebtoken = { version = "10.4.0", features = ["default", "rust_crypto"] }
rand = "0.10.1"
argon2 = "0.5.3"

# --- allocator ---
mimalloc = { version = "0.1.52", features = ["local_dynamic_tls"] }
