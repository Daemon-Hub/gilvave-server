[package]
name = "gilvave-realtime"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
tokio.workspace = true

serde.workspace = true
uuid.workspace = true

tracing.workspace = true

gilvave-core = { path = "../core" }